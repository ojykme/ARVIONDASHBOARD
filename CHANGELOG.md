# Changelog

## Unreleased — 2026-09-22

### Added
- ARVIONSTREAM 응답 상태 계약 문서와 2026-09-22 작업 일지를 추가했다.

### Changed
- 백그라운드 요청 상태와 대시보드 표시를 정리했다.
- 대시보드 컨트롤·스타일을 ARVIONSTREAM Admin V2의 상태 표현과 맞췄다.

### Operations
- 커밋 `752bb9b`를 `main` 브랜치에 푸시했다.

## 이전 Unreleased — 2026-09-14

### 수정
- 동영상 응답 헤더를 요청 완료가 아닌 응답 시작 시 수집하여 스트리밍 중에도 대시보드에 표시한다.
- ARVION 캐시·작업·원본 크기 메타데이터가 있는 미디어 응답의 수집 조건을 보완한다.
- Content-Type, 포맷명, URL 확장자를 함께 확인하여 MP4 등 동영상을 판별한다.
- 새 탭 열기 대신 원본·최적화 동영상을 상세 비교 화면의 네이티브 플레이어에서 재생한다. 닫을 때 재생과 로딩을 정리한다.
- 같은 탭의 같은 동영상·응답 상태·표현에 대한 Range/탐색 요청을 한 행으로 묶어 파일 용량의 중복 합산을 방지한다. 내부 requestCount에는 요청 횟수를 보존한다.
- Content-Range 전체 크기를 파일 크기의 대체값으로 사용한다. 부분 응답 길이를 전체 파일 크기로 표시하지 않는다.
- 다른 URL 쿼리, 탭, 캐시 상태 및 표현은 합치지 않는다. MISS-ASYNC-QUEUED 등 초기 상태를 그대로 보존한다.

### 검증
- `node --test tests/preview.test.cjs`: 2개 통과. 병렬 미리보기 정리, Range 중복 집계, 상태·탭·쿼리 분리와 이미지 수집 유지 확인.
- JavaScript 구문 검사 및 `git diff --check` 통과.
- 이번 작업 중 기존 dashboard.e2e.cjs 통과: 이미지 확대·동기화·비교 모드·전체화면·선호도 유지 확인.
- 이번 작업 중 video.e2e.cjs 통과: Chrome에서 실제 MP4 원본·최적화 URL의 720×720, 약 7.08초 메타데이터와 재생·닫기 정리 확인. Chrome 확장 API는 테스트 대역을 사용하므로 실제 확장 설치 E2E와 구분한다.
- 사용자 화면에서 MP4 행 표시 확인. 중복 집계 수정 후 실제 설치 환경의 재확인은 아직 미완료다.

### 참고
- 동영상 파일 크기 집계는 네트워크의 실제 Range 전송 바이트 누적량과 다르다.
- 영상 재생은 브라우저의 코덱 지원과 원본 서버 접근 조건에 영향을 받는다.
- 배포 버전 및 설치 ZIP은 변경하지 않았다. 적용 시 확장앱을 다시 로드하고 DevTools와 대상 페이지를 다시 연다.
- 문제 발생 시 이 변경 커밋을 되돌리고 확장앱을 다시 로드한다.
